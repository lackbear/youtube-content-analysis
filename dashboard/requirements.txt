# Dashboard deps (kept separate from collector + dbt requirements).
# Streamlit pulls in pandas + altair transitively; pinning them explicitly
# below makes the smoke test reproducible.
streamlit>=1.30,<2.0
duckdb>=1.0
altair>=5.0
pandas>=2.0
# Pipeline Ops page calls the Airflow REST API (POST /api/v1/dags/{id}/dagRuns).
# We avoid shelling out to the airflow CLI — REST is the supported boundary.
requests>=2.31
